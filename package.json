{
  "name": "ddm-subgraph-gysr",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gaurav21/ddm-subgraph-gysr",
    "create-local": "graph create --node http://localhost:8020/ gaurav21/ddm-subgraph-gysr",
    "remove-local": "graph remove --node http://localhost:8020/ gaurav21/ddm-subgraph-gysr",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gaurav21/ddm-subgraph-gysr"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
